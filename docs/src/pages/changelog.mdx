import ChangelogMd from "../data/log.md";
import DocLayout from "../components/DocLayout";

export default ({ location }) => {
  return (
    <DocLayout title="Changelog" location={location} noElevation path="/changelog">
      <ChangelogMd />
    </DocLayout>
  );
};
