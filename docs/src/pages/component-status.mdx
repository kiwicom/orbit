import { ComponentStatusList } from "../components/ComponentStatus";
import DocLayout from "../components/DocLayout";

export default ({ children, location }) => {
  return (
    <DocLayout
      description="A detailed breakdown of our components and the status of their implementation."
      title="Component status 🚀"
      location={location}
      path="/component-status/"
    >
      {children}
    </DocLayout>
  );
};

This listing reflects the current state of our components.

The individual phases don't directly depend on one another.
The design or documentation could be published before development.

<ComponentStatusList />
