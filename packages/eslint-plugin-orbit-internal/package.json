{
  "name": "@kiwicom/eslint-plugin-orbit-internal",
  "version": "0.2.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf ./dist && babel ./src --extensions '.ts' --out-dir ./dist",
    "postinstall": "yarn build"
  },
  "dependencies": {
    "@babel/types": "=7.12.10"
  },
  "devDependencies": {
    "@types/eslint": "^7.2.6"
  }
}
