{
  "imports": "import ChoiceGroup from \"@kiwicom/orbit-components/lib/ChoiceGroup\";\nimport Checkbox from \"@kiwicom/orbit-components/lib/Checkbox\";\nimport Stack from \"@kiwicom/orbit-components/lib/Stack\";",
  "example": "() => {\n  const [checked, setChecked] = React.useState({\n    plane: false,\n    train: false,\n    auto: false,\n  })\n  return (\n    <Stack>\n      <ChoiceGroup\n        label=\"Transport to search\"\n        labelAs=\"h4\"\n        onChange={(event) => {\n          const { name } = event.target\n          setChecked((prevState) => ({ ...prevState, [name]: !checked[name] }))\n        }}\n      >\n        <Checkbox name=\"plane\" label=\"Planes\" checked={checked.plane} />\n        <Checkbox name=\"train\" label=\"Trains\" checked={checked.train} />\n        <Checkbox name=\"auto\" label=\"Automobiles\" checked={checked.auto} />\n      </ChoiceGroup>\n      <ChoiceGroup\n        label=\"Transport to search\"\n        labelSize=\"large\"\n        labelAs=\"h5\"\n        onChange={(event) => {\n          const { name } = event.target\n          setChecked((prevState) => ({ ...prevState, [name]: !checked[name] }))\n        }}\n      >\n        <Checkbox name=\"plane\" label=\"Planes\" checked={checked.plane} />\n        <Checkbox name=\"train\" label=\"Trains\" checked={checked.train} />\n        <Checkbox name=\"auto\" label=\"Automobiles\" checked={checked.auto} />\n      </ChoiceGroup>\n    </Stack>\n  )\n}\n",
  "info": {
    "title": "Labels",
    "description": "Labels for choice groups come in two sizes: normal (the default) and large. You can also control what element the label will render as. The label element will always be connected to the group using aria-labelledby."
  }
}
