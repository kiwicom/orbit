// @flow
import * as React from "react";

export type Translations = {
  [key: string]: string,
};

export type Props = {|
  +values: Translations,
  +children: React.Node,
|};

export type Dictionary = React.ComponentType<Props>;

type WithDictionary<A, B> = (a: React.ComponentType<A>) => React.ComponentType<B>;

declare export default Dictionary;

declare export var withDictionary: WithDictionary<any, any>;
