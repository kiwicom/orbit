// @flow
import type { CSSRules } from "styled-components";

import type { Theme } from "../../../defaultTheme";
import type { Dimensions as PositionRaw } from "../../../utils/boundingClientRect";

export type ResolveTooltipArrowPosition = ({|
  theme: Theme,
  position: string,
  inlineLabel: boolean,
  content: PositionRaw,
  icon?: PositionRaw,
|}) => CSSRules;

declare export default ResolveTooltipArrowPosition;
