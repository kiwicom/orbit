// @flow
import type { CSSRules } from "styled-components";

import type { Theme } from "../../defaultTheme";
import type { Dimensions as PositionRaw } from "../../utils/boundingClientRect";

export type ResolveTooltipPosition = ({|
  theme: Theme,
  position: string,
  inlineLabel: boolean,
  iconBounding: PositionRaw,
  bounding: PositionRaw,
|}) => CSSRules;

declare export default ResolveTooltipPosition;
