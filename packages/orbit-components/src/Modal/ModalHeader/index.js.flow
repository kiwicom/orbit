// @flow
import * as React from "react";
import type { StyledComponent } from "styled-components";

import type { Globals } from "../../common/common.js.flow";

export type Props = {|
  ...Globals,
  +children?: React.Node,
  +illustration?: React.Node,
  +title?: React.Node,
  +description?: React.Node,
  +suppressed?: boolean,
|};

declare export default React.ComponentType<Props>;

declare export var MobileHeader: StyledComponent<any>;

declare export var StyledModalHeader: StyledComponent<any>;

declare export var ModalHeading: StyledComponent<any>;
