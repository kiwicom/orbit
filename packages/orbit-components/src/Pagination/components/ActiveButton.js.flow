// @flow

import * as React from "react";

import type { Sizes } from "..";

export type Props = {|
  children: React.Node,
  transparent?: boolean,
  size?: Sizes,
|};

declare export default React.ComponentType<Props>;
