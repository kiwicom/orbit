// @flow

import * as React from "react";

import type { Sizes } from "../index";

export type Props = {|
  children: React.Node,
  transparent?: boolean,
  size?: Sizes,
|};

export type ActiveButton = React.ComponentType<Props>;

declare export default ActiveButton;
