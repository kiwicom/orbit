// @flow
import * as React from "react";

import type { Sizes } from "../index";

export type Props = {|
  pageCount: number,
  selectedPage: number,
  onPageChange: number => void,
  size: Sizes,
|};

export type CompactPages = React.ComponentType<Props>;

declare export default CompactPages;
