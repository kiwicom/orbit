// @flow
import * as React from "react";

import type { OnPageChange, PageCount, SelectedPage, Sizes } from "../index";

export type Props = {|
  ...OnPageChange,
  ...PageCount,
  ...SelectedPage,
  enlargement?: number,
  size: Sizes,
|};

export type Pages = React.ComponentType<Props>;

declare export default Pages;
