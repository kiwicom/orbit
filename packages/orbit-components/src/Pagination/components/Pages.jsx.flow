// @flow
import * as React from "react";

import type { OnPageChange, PageCount, SelectedPage, Sizes } from "..";

export type Props = {|
  ...OnPageChange,
  ...PageCount,
  ...SelectedPage,
  enlargement?: number,
  size: Sizes,
|};

declare export default React.ComponentType<Props>;
