// @flow

export type Context = {|
  +basis: number,
  +hasError: boolean,
  +desktopRadio: boolean,
  +isContent: boolean,
|};

export type PricingTableContext = React.Context<Context>;

declare export default PricingTableContext;
