export enum SIZES {
  SMALL = "small",
  NORMAL = "normal",
}

export enum STATES {
  DEFAULT = "default",
  HOVER = "hover",
  ACTIVE = "active",
}

export enum TYPES {
  COLORED = "colored",
  NEUTRAL = "neutral",
}
