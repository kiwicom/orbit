// @flow
import * as React from "react";

export type Props = {|
  children?: React.Node,
  noPadding: boolean,
  withPointer?: boolean,
  withBorder?: boolean,
  useMargins?: boolean,
|};

declare export default React.AbstractComponent<Props, HTMLDivElement>;
