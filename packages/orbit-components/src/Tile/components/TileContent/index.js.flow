// @flow
import * as React from "react";

export type Props = {|
  children?: React.Node,
  noPadding: boolean,
  withPointer?: boolean,
  withBorder?: boolean,
  useMargins?: boolean,
|};

export type TileContent = React.ComponentType<Props>;

declare export default React.AbstractComponent<Props, HTMLDivElement>;
