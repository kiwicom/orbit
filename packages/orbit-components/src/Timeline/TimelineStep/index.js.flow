// @flow
import * as React from "react";

export type Type = "success" | "warning" | "critical";
export type Props = {|
  +children: React.Node,
  +subLabel: React.Node,
  +label: string,
  +type?: Type,
|};

export type TimelineStep = React.ComponentType<Props>;

declare export default TimelineStep;
