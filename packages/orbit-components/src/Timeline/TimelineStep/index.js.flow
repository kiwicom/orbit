// @flow
export type Status = "success" | "warning" | "critical";
export type Props = {|
  +children: React$Node,
  +time: string,
  +step: string,
  +status?: Status,
|};

declare export default React$ComponentType<Props>;
