// @flow
import * as React from "react";

import type { Globals } from "../common/common.js.flow";

export type Props = {|
  children: React.Node,
  maxWidth?: string,
  ...Globals,
|};

export type Truncate = React.ComponentType<Props>;

declare export default Truncate;
