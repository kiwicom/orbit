// @flow
import { defaultTokens } from "@kiwicom/orbit-design-tokens";

export default {
  orbit: defaultTokens,
  transitions: true,
  lockScrolling: true,
  lockScrollingBarGap: false,
  rtl: false,
};
