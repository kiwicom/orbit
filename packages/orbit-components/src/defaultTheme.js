// @flow
import { defaultTheme } from "@kiwicom/orbit-design-tokens";

export default {
  orbit: defaultTheme,
  transitions: true,
  lockScrolling: true,
  lockScrollingBarGap: false,
  rtl: false,
};
