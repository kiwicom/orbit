// @flow
import * as React from "react";

export type Context = {|
  +basis?: number | string,
  +hasError?: boolean,
  +desktopRadio?: boolean,
  +isContent?: boolean,
|};

export type PricingTableContextType = React.Context<Context>;

declare export default PricingTableContextType;
