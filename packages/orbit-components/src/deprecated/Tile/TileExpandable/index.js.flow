// @flow
import * as React from "react";

export type State = {|
  contentHeight: number,
|};

export type Props = {|
  +expanded?: boolean,
  +initialExpanded?: boolean,
  +children: React.Node,
|};

export type TileExpandable = React.ComponentType<Props>;

declare export default TileExpandable;
