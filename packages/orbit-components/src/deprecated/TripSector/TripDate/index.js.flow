// @flow
import * as React from "react";

import type { Globals, Translation } from "../../../common/common.js.flow";

export type Props = {|
  ...Globals,
  +children: React.Node,
  +duration?: Translation,
|};

export type TripDate = React.ComponentType<Props>;

declare export default TripDate;
