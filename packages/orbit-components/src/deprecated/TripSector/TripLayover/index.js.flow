// @flow
import * as React from "react";

import type { Globals } from "../../../common/common.js.flow";

export type Props = {|
  ...Globals,
  +children: React.Node,
|};

export type TripLayover = React.ComponentType<Props>;

declare export default TripLayover;
