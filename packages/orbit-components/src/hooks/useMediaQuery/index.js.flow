// @flow

export type QueryMap<T> = {|
  +isMediumMobile: T,
  +isLargeMobile: T,
  +isTablet: T,
  +isDesktop: T,
  +isLargeDesktop: T,
  +prefersReducedMotion: T,
|};

declare function useMediaQuery(): QueryMap<null | boolean>;

export default useMediaQuery;
