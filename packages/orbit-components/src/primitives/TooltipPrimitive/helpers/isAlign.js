// @flow
import { ALIGNS } from "../consts";

export const isAlignCenter = (a: string) => a === ALIGNS.CENTER;

export const isAlignStart = (a: string) => a === ALIGNS.START;

export const isAlignEnd = (a: string) => a === ALIGNS.END;
