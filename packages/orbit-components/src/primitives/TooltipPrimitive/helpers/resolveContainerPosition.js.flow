// @flow
import type { CSSRules } from "styled-components";

import type { Dimensions, Position } from "..";
import type { ThemeProps } from "../../../defaultTheme";

export type Props = {|
  ...Dimensions,
  ...Position,
  ...ThemeProps,
|};

type ResolveContainerPosition = Props => CSSRules | null;

declare export default ResolveContainerPosition;
