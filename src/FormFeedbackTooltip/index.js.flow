// @flow
import type { Globals, UseRef } from "../common/common.js.flow";

export type Props = {
  children: React$Node,
  shown?: boolean,
  isHelp?: boolean,
  boundingRef?: UseRef,
  iconBoundingRef?: UseRef,
  inlineLabel?: React$Node,
  onClick?: () => void | Promise<any>,
  ...Globals,
};

declare export default React$ComponentType<Props>;
