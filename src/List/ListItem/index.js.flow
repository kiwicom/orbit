// @flow
import { defaultTokens } from "@kiwicom/orbit-design-tokens";

export type Props = {|
  +children: React$Node,
  +icon?: React$Element<any>,
  +theme?: typeof defaultTokens,
|};

declare export var Item: React$ComponentType<>;
declare export default React$ComponentType<Props>;
