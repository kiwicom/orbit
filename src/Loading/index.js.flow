// @flow
import type { Globals } from "../common/common.js.flow";

type Type = "buttonLoader" | "boxLoader" | "searchLoader" | "pageLoader";

export type Props = {|
  +children?: React$Node,
  +loading?: boolean,
  +type?: Type,
  +text?: string,
  ...Globals,
|};

declare export var StyledSpinner: React$ComponentType<>;
declare export var StyledLoading: React$ComponentType<>;

declare export default React$ComponentType<Props>;
