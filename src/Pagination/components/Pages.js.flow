// @flow
import type { OnPageChange, PageCount, SelectedPage } from "../index";

export type Props = {|
  ...OnPageChange,
  ...PageCount,
  ...SelectedPage,
  enlargement?: number,
|};

declare export default React$ComponentType<Props>;
