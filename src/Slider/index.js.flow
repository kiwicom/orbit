// @flow

import type { Translation, Globals } from "../common/common.js.flow";
import type { Data } from "./components/Histogram/index";

export type Value = number | number[];

export type SliderCallback = (value: Value) => void | Promise<any>;

export type Props = {|
  ...Globals,
  min?: number,
  max?: number,
  step?: number,
  defaultValue?: Value,
  onChange?: SliderCallback,
  onBeforeChange?: SliderCallback,
  onChangeAfter?: SliderCallback,
  label: Translation,
  description?: Translation,
  histogramData?: Data,
  leftDescription?: Translation,
|};

export type State = {|
  value: Value,
  handleIndex: ?number,
  focused: boolean,
|};

declare export default React$ComponentType<Props>;
