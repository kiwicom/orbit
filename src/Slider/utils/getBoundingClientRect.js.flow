// @flow
type Rect = {|
  bottom: number,
  height: number,
  left: number,
  right: number,
  top: number,
  width: number,
|};

export type GetBoundingClientRect = (ref: React$ElementRef<*>) => ?Rect;

declare export default GetBoundingClientRect;
