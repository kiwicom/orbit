// @flow
import type { Globals } from "../common/common.js.flow";

export type Props = {|
  +compact?: boolean,
  +children: React$Node,
  ...Globals,
|};

export type State = {|
  showShadows: boolean,
  showLeft: boolean,
  showRight: boolean,
|};

declare export default React$ComponentType<Props>;
