// @flow
import List from "../List";

export type TripType = "airline" | "bus" | "train";

export type State = {|
  shown: boolean,
|};

type TripInfo = {|
  code: string,
  time: string,
  city: string,
|};

export type Props = {|
  +children: React$Element<typeof List>,
  +code: string,
  +name: string,
  +departure: TripInfo,
  +arrival: TripInfo,
  +duration: string | number,
  +type: TripType,
  +onClick?: () => void,
|};

declare export default React$ComponentType<Props>;
