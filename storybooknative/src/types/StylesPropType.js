// @flow

/**
 * Styles are only reexported here to avoid importing internal RN libraries
 * everywhere (types are not exported directly).
 */

import {
  type ____DangerouslyImpreciseStyleProp_Internal, // eslint-disable-line camelcase
} from "react-native/Libraries/StyleSheet/StyleSheetTypes";

export type StylePropType = ____DangerouslyImpreciseStyleProp_Internal; // eslint-disable-line camelcase
